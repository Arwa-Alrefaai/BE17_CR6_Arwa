<form method="post" action="{{ path('app_big_events_delete', {'id': big_event.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ big_event.id) }}">
    <button class="btn">Delete</button>
</form>
